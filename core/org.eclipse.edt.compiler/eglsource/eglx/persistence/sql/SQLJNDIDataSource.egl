package eglx.persistence.sql;

externalType SQLJNDIDataSource extends SQLDataSource type NativeType
	private constructor();
	constructor(jndiName string in);
	constructor(jndiName string in, properties Dictionary in);
end
